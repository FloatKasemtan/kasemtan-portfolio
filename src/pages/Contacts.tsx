import React from "react";

const Contacts: React.FC = () => {
  return (
    <div className="h-screen">
      <div className="flex-center font-header topic">Contact</div>
    </div>
  );
};

export default Contacts;
